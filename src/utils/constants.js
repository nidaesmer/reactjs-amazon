export const BASE_URL = "..";
export const GB_CURRENCY = Intl.NumberFormat("en-GB", {
  style: "currency",
  currency: "GBP",
});
/* export const TRY_CURRENCY = Intl.NumberFormat("tr-TR", {
  style: "currency",
  currency: "TRY",
});
export const USD_CURRENCY = Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "USD",
});
*/
